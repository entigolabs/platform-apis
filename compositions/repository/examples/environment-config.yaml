apiVersion: apiextensions.crossplane.io/v1beta1
kind: EnvironmentConfig
metadata:
  name: platform-apis-artifact
data:
  awsProvider: crossplane-aws
  awsRegion: eu-north-1
  dataKMSKey: data
# tags:
#   environment: dev
# scanOnPush: bool
# imageTagMutability: string
# Default ECR lifecycle policy for every repository in this environment. Repositories can override
# the whole list through spec.lifecycleRules. Omit it entirely to create no lifecycle policy at all.
# lifecycleRules:
#   - untagged: true
#     expireAfterDays: 7
#   - keepCount: 20
