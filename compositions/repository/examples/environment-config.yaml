apiVersion: apiextensions.crossplane.io/v1beta1
kind: EnvironmentConfig
metadata:
  name: platform-apis-repository
data:
  awsProvider: crossplane-aws
  awsRegion: eu-west-1
  dataKMSKey: data
# scanOnPush: bool
# imageTagMutability: string