# functions need to be installed separately, function dependencies are defined in crossplane.yaml.
# function definitions here are intended for development purposes and for use with crossplane render command.

apiVersion: pkg.crossplane.io/v1beta1
kind: Function
metadata:
  name: function-kcl
spec:
  package: xpkg.upbound.io/crossplane-contrib/function-kcl:v0.11.6
