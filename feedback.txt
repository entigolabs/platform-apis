[11:37 AM]
- step: auto-ready
  functionRef:
    name: function-auto-ready
This is no longer necessary because our functions' base code does exactly the same thing itself.

[11:37 AM]
- provider: xpkg.upbound.io/crossplane-contrib/function-auto-ready
  version: ">=v0.5.0"
This will also disappear, of course.

[11:41 AM]
In your Dockerfile you have:
FROM --platform=${BUILDPLATFORM} golang:1.25-alpine AS build
But in go.mod it is:
go 1.26.1
Generally, I tend to wait before starting to use the latest Golang version. Bug fixes and tooling updates take time. It isn't strictly wrong to use Go 1.26, but then the Dockerfile must definitely use the same version, and the function builder workflows, golangci, etc., must also be compatible with that version. If you want to avoid that hassle, it’s easier to go back to 1.25 in go.mod.

[11:45 AM]
I am the wrong person to talk to about this, but changes to the run-tests.sh file must be coordinated with Jevgeni. It’s not worth messing with it unless there is a good reason.

[11:49 AM]
You have deleted the helm/templates/kafka.yaml file.
Actually, there should be 2 files in the helm directory: one for the function and one for Kafka. The function file contains things necessary for the function, and the Kafka file contains things specifically for Kafka. At a minimum, for example, the assignment of necessary permissions for Kafka should probably be in the Kafka file:

YAML
apiVersion: apiextensions.crossplane.io/v1alpha1
kind: ManagedResourceActivationPolicy
metadata:
  name: platform-apis-queue-kafka
  annotations:
    argocd.argoproj.io/sync-wave: "-5"
spec:
  activate:
    - clusters.kafka.aws.upbound.io
    - topics.topic.kafka.m.crossplane.io
    - topics.topic.kafka.crossplane.io
    - accesscontrollists.acl.kafka.m.crossplane.io
    - accesscontrollists.acl.kafka.crossplane.io
    - clusterproviderconfigs.kafka.m.crossplane.io
    - clusterproviderconfigusages.kafka.m.crossplane.io
    - secrets.secretsmanager.aws.m.upbound.io
    - secretversions.secretsmanager.aws.m.upbound.io
    - secretpolicies.secretsmanager.aws.m.upbound.io
    - singlescramsecretassociations.kafka.aws.m.upbound.io
[11:53 AM]
What was the reason for needing an exception for Kafka in the helm.yml workflow?
# Update queue.kafka.package.tag — nested path, handled separately from the loop
This should ideally work exactly like the others. (edited)

[11:55 AM]
The same previous question regarding build-helm.yaml. Writing an exception like this suggests more that something is wrong with the solution somewhere.

[11:56 AM]
Also, the Kafka composition should still remain in the list in that file. The others also have both functions and composition packages together:
["artifact"]="platform-apis-artifact-fn"
... [list continues] ...

[11:58 AM]
When running go mod tidy in the queue folder, it seemed like it wanted to change something. go mod tidy should always be performed on the dependency file by yourself. (edited)

[12:01 PM]
The base code sets Ready: true for kube-native objects itself; this is not necessary:

Go
func (g *GroupImpl) GetReadyStatus(observed *composed.Unstructured) resource.Ready {
	switch observed.GetKind() {
	case "Secret":
		return resource.ReadyTrue
	case "ClusterProviderConfig":
		return resource.ReadyTrue
	default:
		return ""
	}
}
You can just return an empty string.

Märt Erlenheim [12:17 PM]
kafkaNamespace = "crossplane-kafka"
This should come from environmentConfig, not be a hardcoded value. We have had an agreement with the AWS provider that the provider name and namespace are the same.

[12:19 PM]
Now that I've looked at your code, I think you should indeed take the aws provider dependency. Using unstructured items isn't wrong, but their management is worse in terms of "noise" compared to objects obtained from the provider.

[12:22 PM]
Up until now, we have had an agreement that the config KMS key name also comes from environmentConfig.
Your solution:
resources["KMSConfigKey"] = base.RequiredKMSKey("config", env.AWSProvider)
vs elsewhere:
resources["KMSConfigKey"] = base.RequiredKMSKey(env.ConfigKMSKey, env.AWSProvider)

[12:24 PM]
And later, using it in the service layer, you can do:

Go
var kmsConfigKey kmsmv1beta1.Key
if err := base.ExtractRequiredResource(required, "KMSConfigKey", &kmsConfigKey); err != nil {
    return nil, err
}
Then there is no need to deal with unstructured querying again:

Go
kmsKeyID := ""
if kmsResources, ok := required["KMSConfigKey"]; ok && len(kmsResources) > 0 {
    keyID, _, _ := unstructured.NestedString(kmsResources[0].Resource.Object, "status", "atProvider", "keyId")
    kmsKeyID = keyID
}
[12:28 PM]
For better readability, nested if statements should be avoided if possible. Early return logic can be used for this. It makes the function flow a bit easier to understand.
[Examples of code refactoring provided in the original text]
I wrote this in a hurry, so I don't promise everything is correct.

[12:32 PM]
I see you are outputting namespaced objects that do not have a namespace assigned. This means they will go into the default namespace. So far, our agreement has been to use namespaced objects such that the created objects go into the namespace of the composite resource (the parent object). This is important for permission distribution, especially when a zone is in use. I don't know if the Kafka provider objects are namespaced or not. Generally, you should perhaps have this discussion about namespaces with Rein, as it is important to do it correctly.

[12:35 PM]
Generally, the code looks correct. Since I don't know the specifics of Kafka objects, I can't say if anything might be wrong there. You should have enough feedback now to proceed. Let me know if questions have arisen or when you have finished the changes. I would then review your code again with fresh eyes.

Oh right, I already forgot this one. As far as I know, compositions should now use a different apiVersion. In the folder compositions/kafka/apis, kafka.entigo.com should be replaced with the value queue.entigo.com. At least until now, our compositions have received the group name from the function.
