apiVersion: pkg.crossplane.io/v1
kind: Configuration
metadata:
  name: platform-apis-cronjob
spec:
  package: {{ .Values.cronjob.package.image }}:{{ .Values.cronjob.package.tag }}
  skipDependencyResolution: true
{{- end }}
