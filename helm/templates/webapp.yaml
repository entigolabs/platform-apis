{{- if .Values.webapp.install }}
apiVersion: pkg.crossplane.io/v1
kind: Configuration
metadata:
  name: platform-apis-webapp
spec:
  package: {{ .Values.webapp.package.image }}:{{ .Values.webapp.package.tag }}
  skipDependencyResolution: true
{{- end }}
