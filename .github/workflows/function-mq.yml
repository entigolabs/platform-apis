name: Build MQ Function

on:
#  push:
#    branches:
#      - main
#    paths:
#      - 'functions/mq/**'
#      - 'functions/common/base/**'
#  pull_request:
#    paths:
#      - 'functions/mq/**'
#      - 'functions/common/base/**'
  workflow_dispatch:
    inputs:
      version:
        description: Package version (e.g. v0.1.0)
        required: false

jobs:
  build:
    uses: ./.github/workflows/builder-function.yml
    with:
      path: functions/mq
      name: platform-apis-mq-fn
      version: ${{ inputs.version }}
    secrets: inherit