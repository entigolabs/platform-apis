name: Build PostgreSQL Function

on:
  push:
    branches:
      - main
    paths:
      - 'functions/postgresql/**'
      - 'functions/common/base/**'
  workflow_dispatch:
    inputs:
      version:
        description: Package version (e.g. v0.1.0)
        required: false

jobs:
  build:
    uses: ./.github/workflows/builder-function.yml
    with:
      path: functions/postgresql
      name: entigo-postgresql-function
      version: ${{ inputs.version }}
    secrets: inherit