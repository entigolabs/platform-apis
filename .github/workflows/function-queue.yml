name: Build Queue Function

on:
#  push:
#    branches:
#      - main
#    paths:
#      - 'functions/queue/**'
#      - 'functions/common/base/**'
#  pull_request:
#    paths:
#      - 'functions/queue/**'
#      - 'functions/common/base/**'
  workflow_dispatch:
    inputs:
      version:
        description: Package version (e.g. v0.1.0)
        required: false

jobs:
  build:
    uses: ./.github/workflows/builder-function.yml
    with:
      path: functions/queue
      name: platform-apis-queue-fn
      version: ${{ inputs.version }}
    secrets: inherit
