name: Build RabbitMQ Composition

on:
  workflow_dispatch:
#  push:
#    branches: [main]
#    paths:
#      - 'compositions/rabbitmq/**'
#      - '!compositions/rabbitmq/examples/**'
#  pull_request:
#    paths:
#      - 'compositions/rabbitmq/**'
#      - '!compositions/rabbitmq/examples/**'

jobs:
  build:
    uses: ./.github/workflows/builder-composition.yml
    with:
      path: compositions/rabbitmq
      name: platform-apis-rabbitmq
    secrets: inherit