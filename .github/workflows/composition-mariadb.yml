name: Build MariaDB Composition

on:
  workflow_dispatch:
#  push:
#    branches: [main]
#    paths:
#      - 'compositions/mariadb/**'
#      - '!compositions/mariadb/examples/**'
#  pull_request:
#    paths:
#      - 'compositions/mariadb/**'
#      - '!compositions/mariadb/examples/**'

jobs:
  build:
    uses: ./.github/workflows/builder-composition.yml
    with:
      path: compositions/mariadb
      name: platform-apis-mariadb
    secrets: inherit
