apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: test-postgresql
  namespace: a-apps
spec:
  destination:
    server: https://kubernetes.default.svc
    namespace: test-postgresql
  project: a
  source:
    chart: platform-apis-test-postgresql
    repoURL: ghcr.io/entigolabs
    targetRevision: '*.*.*'
    helm:
      releaseName: postgresql
