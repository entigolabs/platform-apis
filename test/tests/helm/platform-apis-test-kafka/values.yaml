clusterName: test-kafka-cluster
clusterARN: ""           # Set after MSK cluster is created and ARN is retrieved
renderCompositions: false # false = render SG + cluster; true = render MSK observer + topic + user