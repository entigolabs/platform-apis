apiVersion: tenancy.entigo.com/v1alpha1
kind: Zone
metadata:
  name: {{ .Name }}
spec:
  namespaces:{{ if not .Namespaces }} []
{{ else }}
{{ range .Namespaces }}  - name: {{ . }}
{{ end }}{{ end }}  pools:
  - name: test
    requirements:
    - key: capacity-type
      value: ON_DEMAND
